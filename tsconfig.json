/* To learn more about this file see: https://angular.io/config/tsconfig. */

// this needs to be in `tsconfig.json` to be picked up by VS Code
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "vitest/globals"
    ]
  },
  "files": [
    "src/test-setup.ts",
    "src/main.ts",
  ],
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.d.ts",
  ],
}
